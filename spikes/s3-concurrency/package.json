{
  "name": "@chromatrix/spike-s3-concurrency",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Spike S3: shared-tab concurrency vs per-job isolated browser contexts on one identity — what breaks, and does context isolation break the persistent login. See docs/PRD.md §7.",
  "scripts": {
    "start": "node --import @swc-node/register/esm-register src/run.ts",
    "typecheck": "tsgo -p tsconfig.json"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/ws": "^8.5.13"
  }
}
