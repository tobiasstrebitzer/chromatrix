// @chromatrix/core — identity registry, session/tab-pool orchestration, profile locking, health, reaper.
// See docs/PRD.md §4. Populated after S1 (mux) and S3 (concurrency model) land.

export const CORE_PACKAGE = '@chromatrix/core' as const
